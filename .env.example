DATABASE_URL=""
SUPABASE_DB_URL=""

# Database Mode Options:
# - "local": Use only local PostgreSQL
# - "supabase": Use only Supabase
# - "both": Use both databases (sync operations)
DB_MODE=both

# What to do if Supabase fails when DB_MODE=both
# - "fail": Rollback everything and return error
# - "continue": Continue with local DB only and log error
# - "retry": Retry Supabase operation once
SUPABASE_FAILURE_STRATEGY=continue

COHERE_API_KEY=""

SUPABASE_URL=""
SUPABASE_KEY=.""
SUPABASE_BUCKET=""

# JWT Settings (add these if not already present)
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7